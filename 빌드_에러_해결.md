# 빌드 에러 해결 가이드

## 🔍 발견된 빌드 에러

```
Dynamic server usage: Route /api/news couldn't be rendered statically because it used `nextUrl.searchParams`
Dynamic server usage: Route /api/searches couldn't be rendered statically because it used `nextUrl.searchParams`
```

**문제:** Next.js가 API 라우트를 정적으로 렌더링하려고 시도하지만, `nextUrl.searchParams`를 사용하기 때문에 동적 라우트로 처리해야 합니다.

---

## ✅ 해결 방법

API 라우트에 **동적 라우트 설정**을 추가했습니다.

### 수정된 파일들:
- ✅ `app/api/news/route.ts`
- ✅ `app/api/searches/route.ts`
- ✅ `app/api/searches/[id]/route.ts`
- ✅ `app/api/summarize/route.ts`
- ✅ `app/api/chat/route.ts`

### 추가된 코드:
```typescript
// 동적 라우트로 명시 (정적 생성 방지)
export const dynamic = 'force-dynamic';
export const runtime = 'nodejs';
```

---

## 🎯 이것이 해결하는 문제

### Before (문제):
- Next.js가 빌드 시 API 라우트를 정적으로 렌더링하려고 시도
- `nextUrl.searchParams` 사용으로 인해 오류 발생
- 빌드 로그에 경고 메시지 표시

### After (해결):
- API 라우트가 명시적으로 동적 라우트로 선언됨
- 빌드 시 정적 렌더링 시도하지 않음
- 경고 메시지 사라짐

---

## 📋 다음 단계

### 1단계: GitHub에 푸시

수정된 파일들을 GitHub에 푸시:

```bash
# GitHub Desktop 사용
1. File → Add Local Repository
2. github-upload 폴더 선택
3. Commit to main
4. Push origin
```

또는 GitHub 웹사이트에서 직접 업로드

### 2단계: Vercel 자동 재배포

GitHub에 푸시하면 Vercel이 자동으로 재배포합니다.

### 3단계: 빌드 로그 확인

재배포 후:

1. **Vercel Deployments → 최신 배포**
2. **Build Logs 확인**
3. **다음 경고가 사라졌는지 확인:**
   - ❌ "Dynamic server usage: Route /api/news..."
   - ❌ "Dynamic server usage: Route /api/searches..."

---

## ✅ 확인 사항

재배포 후 빌드 로그에서:

1. **"Generating static pages" 성공**
2. **"Dynamic server usage" 경고 없음**
3. **빌드 완료 (Build Completed)**

---

## 🔧 추가 확인

### Prisma 경고도 해결됨

빌드 로그에 나타난 Prisma 경고:
```
prisma:warn In production, we recommend using `prisma generate --no-engine`
```

이것은 경고일 뿐이며 빌드를 막지 않습니다. 필요하면 `package.json`의 `postinstall` 스크립트를 수정할 수 있습니다:

```json
{
  "scripts": {
    "postinstall": "prisma generate --no-engine"
  }
}
```

하지만 현재 설정으로도 정상 작동합니다.

---

## 🎉 완료!

수정된 코드를 GitHub에 푸시하면:
1. 빌드 에러 해결
2. 경고 메시지 사라짐
3. 정상 배포 완료

---

## 📞 문제가 계속되면

1. **빌드 로그의 구체적인 오류 메시지 확인**
2. **환경변수 `POSTGRES_PRISMA_URL` 확인**
3. **재배포 후 다시 확인**

문제가 계속되면 알려주세요!
